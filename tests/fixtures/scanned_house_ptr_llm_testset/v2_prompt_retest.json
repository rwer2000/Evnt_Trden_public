{
  "description": "Re-run of the 6 worst-performing filings from the original 16-filing batch (manifest.json), using a revised prompt (extraction_prompt_v2.txt) that explicitly warns against amount-bracket legends, forbids inventing illegible names, and forbids installing other tools. Run 2026-09-24, same session as the original batch's follow-up work. Every `v2_verify_extraction_result` below is the real, literal output of calling `scan_verification.verify_extraction()` against the parsed extraction (not a hand-reasoned guess) -- confirmed by actually running it against these six files.",
  "model": "claude-haiku-4-5 (--model haiku)",
  "conclusion": "Modest, uneven improvement -- not a fix. One clear win (the McCaul legend-confusion case) and one new regression (a previously-honest empty result became a confidently wrong non-empty one). A third apparent finding -- a 'multi-page Read-tool limitation' on house:9114491 -- turned out on direct verification to be false: the model fabricated that explanation for a 1-page file (confirmed via extract_pages_words()). That correction is itself the headline finding of this re-test: a vision model's own stated reason for failing cannot be trusted without independent verification, the same as its content extractions can't. scan_verification.verify_extraction()'s blank-description check is doing real work regardless -- it independently catches 2 of the 6 filings below (9107579, 9111845) that would otherwise have written bad data.",
  "filings": [
    {
      "filing_id": "house:9107579",
      "v1_verdict": "BAD (fabricated company names: 'Malt Cocktail Wines Inc.', 'Tar Brands Inc.')",
      "v1_n_transactions": 10,
      "v2_n_transactions": 6,
      "v2_cost_usd": 0.1494,
      "v2_blank_descriptions": 2,
      "v2_verify_extraction_result": "ok=False, needs_review=False -- rejected: 2 of 6 rows have a blank asset_description_raw",
      "notes": "No fabricated names this time -- real companies (Analog Devices, Emerson Electric, General Electric, Parker Hannifin). But coverage dropped (10 -> 6) and 2 rows now have a null description instead of a plausible-but-wrong one. Net effect: less confidently wrong, but still incomplete, and still correctly rejected by verify_extraction rather than silently written."
    },
    {
      "filing_id": "house:9107699",
      "v1_verdict": "BAD (missed all of page 1, amounts off by multiple brackets)",
      "v1_n_transactions": 4,
      "v2_n_transactions": 4,
      "v2_cost_usd": 0.0802,
      "v2_blank_descriptions": 0,
      "v2_verify_extraction_result": "ok=True, needs_review=False",
      "notes": "Different 4 rows than v1 (now includes Procter & Gamble), ground truth is 6-8 -- still an undercount, still page-1 content apparently missing. owner is null on every row. Prompt v2 did not fix the multi-page coverage gap."
    },
    {
      "filing_id": "house:9111845",
      "v1_verdict": "FAILED (honest empty result, model said scan quality too poor)",
      "v1_n_transactions": 0,
      "v2_n_transactions": 7,
      "v2_cost_usd": 0.0748,
      "v2_blank_descriptions": 7,
      "v2_verify_extraction_result": "ok=False, needs_review=True -- rejected: 7 of 7 rows blank, PLUS flagged: a single (owner, description, type, date, amount) combination repeats 4 times",
      "notes": "REGRESSION in behavior, not caught by the ground truth count alone: v1's honest 'I can't read this' became v2's confident 7-row array with every description blank -- the exact legend-confusion shape the v2 prompt was written to prevent, on a *different* filing than the one it was written for. verify_extraction() catches this outright, which is the whole point of having it as an independent gate rather than trusting prompt engineering alone."
    },
    {
      "filing_id": "house:9114491",
      "v1_verdict": "FAILED (tried to run apt-get install poppler-utils, refused, gave up)",
      "v1_n_transactions": 0,
      "v2_n_transactions": 0,
      "v2_cost_usd": 0.0718,
      "v2_blank_descriptions": 0,
      "v2_verify_extraction_result": "ok=True, needs_review=True -- flagged: zero transactions extracted",
      "notes": "No longer tries to install a forbidden tool (the explicit instruction worked). Its stated excuse -- 'the Read tool only surfaced page 1 of this multi-page PDF, ~41KB implies more pages' -- was checked directly against the file and is FALSE: `extract_pages_words()` (pdfplumber) confirms this PDF has exactly 1 page. There is no multi-page Read-tool limitation here; the model fabricated a plausible-sounding technical excuse rather than admitting it couldn't parse this filing's actual layout (a column-per-transaction matrix, not row-per-transaction, per scan_style above). This is the same failure family as the invented company names on house:9107579 and house:8218645's hallucinated legend-as-transactions -- confident, specific-sounding fabrication, just aimed at explaining a failure instead of producing one. Do not trust a vision model's own stated reason for a failure without checking it independently."
    },
    {
      "filing_id": "house:8218645",
      "v1_verdict": "BAD (silent data corruption -- the amount-bracket legend (columns A-K) extracted as 14 transactions, every description null)",
      "v1_n_transactions": 14,
      "v2_n_transactions": 20,
      "v2_cost_usd": 0.0654,
      "v2_blank_descriptions": 0,
      "v2_verify_extraction_result": "ok=True, needs_review=False (n=20 is exactly at, not over, the >20 review threshold)",
      "notes": "Clear win: real company names (ABBVIE INC/ABBV, ACCENTURE PLC/ACN, ADOBE INC, ...), no blank descriptions, no legend confusion. The explicit 'a legend is not a transaction table' instruction appears to have worked for the exact failure mode it targeted. Still very likely an undercount against the ~100+ ground truth (this filing has 6 dense pages) -- the mechanical checks here can't catch under-extraction, only garbage rows, so this filing still needs a human page-count sanity check before being trusted."
    },
    {
      "filing_id": "house:8219420",
      "v1_verdict": "FAILED (empty, handwritten filing, gave up even with a longer timeout)",
      "v1_n_transactions": 0,
      "v2_n_transactions": 1,
      "v2_cost_usd": 0.1520,
      "v2_blank_descriptions": 0,
      "v2_verify_extraction_result": "ok=True, needs_review=False -- passes today, but tx_type/tx_date/amount_min/amount_max are all null, which no current check flags; worth a future check for 'too many null fields on a non-empty row'",
      "notes": "Partial win: extracted one transaction ('T-BILL') matching the ground truth count of 1, where v1 gave up entirely. Most other fields are still null. The 'attempt a careful reading rather than giving up' instruction seems to have helped the model try, even if the result is incomplete."
    }
  ],
  "recommendations": [
    "Keep verify_extraction()'s blank-description check as a hard gate -- it independently caught real problems in 2 of these 6 filings under the *improved* prompt, including a new failure shape the prompt change itself introduced on a different filing.",
    "Checked directly (2026-09-24) and CLOSED: house:9114491's claimed 'multi-page Read-tool limitation' was a fabricated excuse, not a real tooling issue -- extract_pages_words() confirms the file has exactly 1 page. No Read-tool/pagination fix is needed. The real, still-open problem for this filing is that a column-per-transaction matrix layout (as opposed to the usual row-per-transaction table) isn't being read correctly at all -- that needs its own investigation as a content/layout problem, not a tooling one.",
    "Add a check for a model's own stated failure explanation before trusting it operationally: this batch shows the same model that invents company names and hallucinates legend rows also invents technical excuses for why it failed. Any pipeline that logs or acts on a model's self-reported reason for an empty/partial result should treat that reason as unverified until checked against the source file, exactly like its positive extractions.",
    "Add an under-extraction signal for large filings: none of the mechanical checks in verify_extraction() catch a plausible-looking but incomplete result (house:8218645's 20-of-100+ and house:9107699's 4-of-6-8). A rough page-count-to-transaction-count ratio, or a required model self-report of 'total rows visible', would help.",
    "Do not treat this prompt revision as validated -- one more filing (9111845) got mechanically worse under it. This needs a larger sample before being adopted as the production prompt, not a 6-filing spot check."
  ]
}
